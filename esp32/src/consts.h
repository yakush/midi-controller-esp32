#pragma once

#include <math.h>
#include "config.h"

static float PI_2 = PI * 2.0;
static float PI_1_2 = PI * 1.0 / 2.0;
static float PI_3_2 = PI * 3.0 / 2.0;

static uint32_t WAVE_PI = (WAVE_SAMPLE_RATE / 2);
static uint32_t WAVE_PI_2 = (WAVE_PI * 2);
static uint32_t WAVE_PI_1_2 = ((WAVE_PI * 1) / 2);
static uint32_t WAVE_PI_3_2 = ((WAVE_PI * 3) / 2);